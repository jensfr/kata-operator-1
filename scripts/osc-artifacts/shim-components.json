{
  "_comment": [
    "OSC downstream payload for upstream kata-deploy.",
    "Uses upstream schema and standard names for shared components.",
    "osc-guest-support is an OSC-specific component: osbuilder, kata-agent,",
    "and boot-time guest regeneration. Kernel and rootfs are not shipped;",
    "guest-prep generates them from the host kernel at install time."
  ],
  "shims": {
    "qemu": {
      "x86_64": [
        "shim-v2-go",
        "qemu",
        "virtiofsd",
        "osc-guest-support"
      ]
    }
  }
}
